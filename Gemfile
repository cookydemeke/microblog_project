source 'https://rubygems.org/'
# == ruby ==
ruby '2.4.1'            # set this version to your default ruby version
# == sinatra ==
gem 'sinatra'
gem 'activerecord'
gem 'sinatra-activerecord'
gem 'rake'
# == db ==
  gem 'sqlite3'
# group :production do
#   gem 'pg'
# end

# == run this command in terminal (LEFT tab) from project folder
# bundle install

# == run this command in terminal (LEFT tab) after running bundle install
# gem install sinatra-contrib

# == run this command in terminal (MIDDLE tab) to launch server
# ruby app.rb
